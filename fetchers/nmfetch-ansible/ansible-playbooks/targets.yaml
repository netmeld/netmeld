all:
  children:
    local:
      hosts:
        127.0.0.1:
          ansible_connection: local
    cisco:
      hosts:
    junos:
      hosts:
    vyos:
      hosts:
    linux:
      hosts:
        sample.host:
          ansible_user: someone
          ansible_password: something
    windows:
      hosts:
